import React from 'react';
import useTableConfig from './useTableConfig';

interface SplitTableHeaderProps {}

const SplitTableHeader = () => {
  //   const {} = useTableConfig();

  return <div>123</div>;
};

SplitTableHeader.defaultProps = {};

export default React.memo(SplitTableHeader);
